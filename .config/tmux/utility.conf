# Display lazygit

# bind -r g display-popup -d '#{pane_current_path}' -w80% -h80% -E lazygit
# bind-key g display-popup -d "#{pane_current_path}" -w 80% -h 80% -E "echo hi"

# INFO: debug
# bind-key g display-message "Fired g"
bind-key g display-popup -d "#{pane_current_path}" -w 80% -h 80% -E "/opt/homebrew/bin/lazygit"
